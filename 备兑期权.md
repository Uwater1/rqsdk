# 2026年A股增强型收益策略执行报告： Project JEPI-CN (Monthly Edition)

**修订日期：** 2026年3月23日
**底层资产：** 沪深300ETF华泰柏瑞 (510300.XSHG)
**策略类型：** 备兑开仓 + 看跌价差组合 (Covered Call + Bull Put Spread)
**数据基础：** 本地 Parquet 数据库 (`data/`)

---

## 1. 策略背景与数据驱动

本策略旨在通过 300ETF 现货底仓结合期权卖方策略，在 2026 年的市场环境下构建稳定的现金流。

### 1.1 核心研究发现 (基于 `data/walkthrough.md`)
根据对 2010 年至今沪深 300 指数 (HS300) 30 日日历回归收益率的分布研究：
- **收益分布偏度**：存在明显的下行偏向。HS300 下跌超过 1 个单位（50点）的概率为 **49.77%**，而上涨超过 1 个单位的概率仅为 **40.15%**。
- **策略启示**：
    - **Covered Call (CC) 侧**：由于上涨概率相对较低且波动较小，OTM 3-5 档的卖出认购期权具有较高的胜率。
    - **Put Spread (PS) 侧**：由于下行偏向严重，必须严格执行技术位支撑确认，并强制购买保护腿以对冲“接飞刀”风险。

---

## 2. 资本结构与头寸配置 (基于本地数据库 510300)

### 2.1 底层资产 (The Equity Leg)
- **持仓标的**：沪深300ETF华泰柏瑞 (510300.XSHG)
- **持仓数量**：20,000 份
- **市值参考**：根据 `data/510300_1d.parquet` 最新成交价计算。
- **功能**：作为 2 张认购期权 (Call) 的备兑抵押。

### 2.2 收入引擎配置
1.  **阶梯式备兑看涨 (Staggered Covered Calls)**：
    - **腿 A (1张)**：OTM 3-4 档。目标获取加速的时间价值衰减。
    - **腿 B (1张)**：OTM 4-5 档。保留部分指数大幅上涨的弹性收益。
    (由 Alpha Finder 分布模型辅助，腿A 可以 等于 腿B 如果某个价位alpha最大)
2.  **技术支撑型看跌价差 (Bull Put Spread)**：
    - 在 HS300 关键技术支撑位（由 Alpha Finder 分布模型辅助）下方卖出 1 张 Put，并买入更深虚值 Put 锁定最大亏损。

---

## 3. 数据与基础设施：利用已有数据库

分析与回测代码应直接调用本地 Parquet 数据库。本策略涉及三类核心数据文件：

1.  **`data/300ETF_instruments.parquet`**：期权合约元数据（行权价、到期日、乘数等）。
2.  **`data/300ETF_historical_prices.parquet`**：期权历史行情（OHLC、持仓量等），通过 `order_book_id` 与合约元数据关联。
3.  **`data/510300_1d.parquet`**：底层 ETF (510300.XSHG) 的日线历史行情。

### 3.1 数据加载与关联示例

```python
import pandas as pd

# 1. 加载期权元数据
inst = pd.read_parquet('data/300ETF_instruments.parquet')

# 2. 加载期权历史行情
opt_prices = pd.read_parquet('data/300ETF_historical_prices.parquet')

# 3. 加载底层 ETF 行情
etf_prices = pd.read_parquet('data/510300_1d.parquet')

# 示例：将期权行情与其行权价(strike_price)关联
# 关联键：order_book_id
merged_opt = opt_prices.merge(inst[['order_book_id', 'symbol', 'strike_price', 'maturity_date', 'option_type']], 
                               on='order_book_id', how='left')

# 示例：获取 510300 ETF 最新收盘价
latest_etf_close = etf_prices.iloc[-1]['close']
```

---

## 4. 模拟期权买卖价差 (Bid-Ask Spread) 方案

在回测中，直接使用撮合价（Mid-price）会高估收益。本策略采用基于机器学习的价差模拟方案 (`spread.py`)：

### 4.1 方案原理
利用预训练的 LightGBM 模型 (`spread_model.txt`)，根据以下特征预测合约的 `log(1 + spread)`：
- **核心特征**：`midprice` (中价), `iv` (隐含波动率), `otm_depth` (虚值深度), `days_to_expire` (剩余天数), `S` (标的价格)。
- **转换逻辑**：
    1.  计算合约的 IV。
    2.  模型预测得到 `predicted_spread`。
    3.  **Bid (买入价)** = `midprice - predicted_spread / 2`
    4.  **Ask (卖出价)** = `midprice + predicted_spread / 2`

### 4.2 执行建议
在回测开仓时，卖出期权应按 **Bid** 价成交，买入期权应按 **Ask** 价成交。

---

## 5. 风险管理与动态调整 (Risk Management)

| 风险维度 | 应对措施 |
| :--- | :--- |
| **严重下行** | 利用 30d 分布模型预警。当 30d 预期收益率分布向左极端偏移时，停止 Put Spread 操作。 |
| **流动性风险** | 仅交易 300ETF 挂牌的主力合约，利用 `spread.py` 动态评估并剔除价差过大的合约。 |
| **上行指派** | 若 ETF 被行权收走，立即转入“现金担保卖 Put (CSP)”模式，确保资金不闲置并等待回调接回底仓。 |

---

## 6. TODO: 进一步研究与优化方向

- [ ] **多品种横向对比**：利用 `data/` 下的 `500ETF` 和 `1000ETF` 数据，对比小盘股指数备兑策略在波动率补偿上的优势。
- [ ] **动态 Delta 对冲**：研究在极端行情下，利用部分现金头寸进行 Delta 动态调整的可行性。
- [ ] **IV Skew 深度挖掘**：结合 `result.md` 中的 `options.get_indicators()`，利用 Skew (偏度) 指标优化 Put Spread 的行权价选择。
- [ ] **分红调整自动化**：针对 300ETF 的分红除权，完善合约属性追踪脚本，防止回测中的价格跳变误差。

---
*免责声明：本报告基于本地量化数据库及模型回测生成。期权交易涉及高度复杂性，投资者应具备相应的风险承受能力。*
